# 关键决策

## 令牌不持久化

- 原因：Session 令牌等同账号凭据，只在用户点击登录时写入 ChatGPT 安全 Cookie，输入框随后清空。
- 放弃方案：不使用插件存储保存历史令牌，也不接入任何远程服务。

## 限制 Cookie 范围

- 原因：插件权限仅覆盖 `chatgpt.com`，减少凭据暴露范围。
- 放弃方案：不申请所有网站权限，不支持向任意域名写入 Cookie。
